import React from 'react';
import styles from './feed.module.css';

const Feed = () => {
    return (
        <main className={styles.feed_main}>
            <h1 className={styles.feed_header}>Лента заказов</h1>
            <section className={styles.feed_section}>

            </section>
        </main>
    );
};

export default Feed;